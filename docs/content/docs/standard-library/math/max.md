+++
title = 'math.max'
linkTitle = 'max'
description = 'Returns the larger of two numbers.'
weight = 0
draft = false
+++

Returns the larger of two numbers.

## Examples

```go
printf("%g\n", math.max(5, 10))  // 10
printf("%g\n", math.max(-5, -2)) // -2
```

