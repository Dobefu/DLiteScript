+++
title = 'io.deleteFile'
linkTitle = 'deleteFile'
description = 'Deletes the given file. Returns an error if the file does not exist. Part of the io namespace.'
weight = 0
draft = false
+++

Deletes the given file. Returns an error if the file does not exist.

## Examples

```go
io.deleteFile("data.txt") // deletes the file called "data.txt"
```
