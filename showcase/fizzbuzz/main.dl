for var i from 1 to 15 {
  var buf string = ""

  if ((i % 3) == 0) {
    buf += "Fizz"
  }

  if ((i % 5) == 0) {
    buf += "Buzz"
  }

  if (buf == "") {
    buf = sprintf("%g", i)
  }

  printf("%s\n", buf)
}
