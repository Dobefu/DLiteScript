import "./utils.dl"

var x number = 5
var y number = 3

var sum number = add(x, y)
var product number = multiply(x, y)

printf("x = %g, y = %g\n", x, y)
printf("x + y = %g\n", sum)
printf("x * y = %g\n", product)
